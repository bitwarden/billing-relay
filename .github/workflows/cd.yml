---
name: CD

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write
  deployments: write
  id-token: write
  checks: write
  pull-requests: write

jobs:
  version:
    uses: ./.github/workflows/_version.yml

  test:
    uses: ./.github/workflows/_test.yml
    with:
      project-name: Billing.Test
      project-path: ./test/Billing.Test

  build:
    needs:
      - test
      - version
    uses: ./.github/workflows/_build.yml
    with:
      project-name: Billing
      project-path: ./src/Billing
      version: ${{ needs.version.outputs.version }}

  build-push-docker:
    needs:
      - test
      - version
      - build
    uses: ./.github/workflows/_docker.yml
    with:
      project-name: Billing
      project-path: ./src/Billing
      version: ${{ needs.version.outputs.version }}
      image-name: billing-relay
      push-docker-image: true
    secrets: inherit

  deploy-k8s-service:
    needs:
      - build-push-docker
      - version
    uses: bitwarden/devops/.github/workflows/_deploy_k8s_service.yml@8f20e30181220b987cca242197ee88c9f9c6512e
    with:
      image-name: billing-relay
      environment: production
      repository: mothership
      tag: ${{ needs.version.outputs.version }}
    secrets: inherit
